#include<iostream>
#include<algorithm>
#include<math.h>
#include<vector>
using namespace std;
int main(){
    int n, m;
    cin >> n >> m;

    int a[m], b[m];
    for(int i = 0; i < m; i++) cin >> a[i], b[i];

    int ans = 0;
    if(m == 0) {
        cout << n << endl;
        return 0;
    }

    
}